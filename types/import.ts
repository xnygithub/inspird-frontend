export interface Board {
    boards: {
        id: string;
        name: string;
        pin_count: number;
        url: string;
        image_cover_hd_url: string;
    }
    count: number;
    username: string;

}